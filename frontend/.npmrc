# Only install exact versions (no auto-updates)
save-exact=true

# Run audit on every install
audit=true

# Block packages with moderate+ vulnerabilities
audit-level=moderate

# Use package-lock for integrity
package-lock=true